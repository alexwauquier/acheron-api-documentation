info:
  name: Get details of an event
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/v1/schedules/{{schedule_id}}/events/{{event_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
