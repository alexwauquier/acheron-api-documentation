info:
  name: Get events of a schedule
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/v1/schedules/{{schedule_id}}/events"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
