info:
  name: List schedules
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/v1/schedules"
  params:
    - name: search
      value: CIR3
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
