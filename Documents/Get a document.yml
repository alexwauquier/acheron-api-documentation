info:
  name: Get a document
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/v1/documents/{{document_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
