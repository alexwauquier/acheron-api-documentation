info:
  name: List account repayments
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/v1/account/repayments"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
