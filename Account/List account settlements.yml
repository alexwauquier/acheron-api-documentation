info:
  name: List account settlements
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/v1/account/settlements"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
