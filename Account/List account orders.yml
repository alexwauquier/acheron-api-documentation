info:
  name: List account orders
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/v1/account/orders"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
