info:
  name: List student absences
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/v1/education/absences"
  params:
    - name: status
      value: unexcused
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
