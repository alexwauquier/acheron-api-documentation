info:
  name: Create a reminder in Google Calendar
  type: http
  seq: 5

http:
  method: POST
  url: "{{base_url}}/v1/google/calendar/reminders"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
