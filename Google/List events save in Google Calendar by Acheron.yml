info:
  name: List events save in Google Calendar by Acheron
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/v1/google/calendar/events"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
