info:
  name: Login Google OAuth2
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/v1/google/auth"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
