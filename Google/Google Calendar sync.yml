info:
  name: Google Calendar sync
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/v1/google/calendar/sync"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
