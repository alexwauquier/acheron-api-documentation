info:
  name: Get user information
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/v1/auth/me"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
