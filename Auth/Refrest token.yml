info:
  name: Refrest token
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/v1/auth/refresh"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
